# `(auth)/register/` — Registration Page

## Purpose
Allows new users to create an EduForge account and start building their learning platform.

## URL
`/register`

## Current Files
- `page.tsx` — Registration page with title and subtitle

## Future Files
- `_components/register-form.tsx` — Full registration form (name, email, password, confirm)
- `_components/plan-selector.tsx` — Plan selection during signup
- `_hooks/use-register.ts` — Registration mutation hook
- `loading.tsx` — Loading skeleton
- `error.tsx` — Error boundary
