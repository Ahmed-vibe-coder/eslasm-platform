# `(dashboard)/agencies/[id]/` — Agency Detail

## Purpose
Management hub for a single agency. Shows agency overview, quick actions, and links to sub-sections (courses, students, trainers, analytics).

## URL
`/agencies/:id`

## Future Files & Folders
- `_components/agency-overview.tsx` — Stats cards and quick actions
- `_components/agency-settings-form.tsx` — Edit agency details
- `courses/page.tsx` → `/agencies/:id/courses` — Course management
- `students/page.tsx` → `/agencies/:id/students` — Student management
- `trainers/page.tsx` → `/agencies/:id/trainers` — Trainer management
- `analytics/page.tsx` → `/agencies/:id/analytics` — Agency analytics
- `settings/page.tsx` → `/agencies/:id/settings` — Agency settings (branding, domain)
