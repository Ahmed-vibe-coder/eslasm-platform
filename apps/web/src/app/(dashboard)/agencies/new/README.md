# `(dashboard)/agencies/new/` — Create Agency

## Purpose
Multi-step form to create a new learning platform (agency). Collects name, subdomain, branding, and initial template selection.

## URL
`/agencies/new`

## Future Files
- `_components/agency-form.tsx` — Main form with react-hook-form + zod
- `_components/subdomain-input.tsx` — Subdomain field with availability check
- `_components/template-picker.tsx` — Visual template selection grid
- `_components/branding-step.tsx` — Logo upload and color picker
