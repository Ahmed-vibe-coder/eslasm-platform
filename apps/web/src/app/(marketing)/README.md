# `(marketing)/` — Public Marketing Pages

## Purpose
Public-facing pages that don't require authentication. These are the pages visitors see before signing up.

## Role
Converts visitors into users. Uses the marketing layout with site header (nav) and site footer.

## URL Mapping
| Folder | URL | Page |
|---|---|---|
| `page.tsx` | `/` | Homepage with hero section |
| `pricing/` | `/pricing` | Pricing plans comparison |

## Future Pages
- `features/page.tsx` → `/features` — Feature showcase
- `about/page.tsx` → `/about` — About the platform
- `contact/page.tsx` → `/contact` — Contact form
- `blog/page.tsx` → `/blog` — Blog listing
- `blog/[slug]/page.tsx` → `/blog/:slug` — Blog post detail
- `templates/page.tsx` → `/templates` — Public template gallery
- `changelog/page.tsx` → `/changelog` — Product changelog
- `docs/page.tsx` → `/docs` — Public documentation
