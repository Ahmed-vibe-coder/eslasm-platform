# `[tenant]/dashboard/` — Student Dashboard

## Purpose
Personal dashboard for enrolled students. Shows learning progress, enrolled courses, and earned certificates.

## URL
`/dashboard` (on tenant subdomain)

## Future Files
- `_components/progress-overview.tsx` — Learning progress cards
- `_components/enrolled-courses.tsx` — List of enrolled courses with progress bars
- `_components/recent-activity.tsx` — Recent lesson completions and achievements
- `_components/certificates-preview.tsx` — Recently earned certificates
- `_hooks/use-student-progress.ts` — Student progress hooks
