# `[tenant]/login/` — Tenant Login

## Purpose
Login page scoped to this specific agency/tenant. Students sign in to access courses they've enrolled in.

## URL
`/login` (on tenant subdomain)

## Future Files
- `_components/tenant-login-form.tsx` — Login form with tenant branding
