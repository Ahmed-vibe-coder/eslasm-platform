# `api/builder/publish/` — Publish Builder Page

## Purpose
Marks a page as published, making it live on the tenant's subdomain.

## Method
`POST /api/builder/publish`

## Body
```json
{ "pageId": "string" }
```
