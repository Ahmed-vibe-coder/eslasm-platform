# `api/proxy/[...path]/` — Proxy Route Handler

## Purpose
Forwards HTTP requests to the backend, mapping all methods (GET, POST, PUT, PATCH, DELETE).

## Current Files
- `route.ts` — Universal proxy handler
